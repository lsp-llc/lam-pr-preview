# PR #84 preview — Community Analytics Monday snapshot

**Draft only. Do not merge from this preview. Not production. Not a Firebase deploy.**

Public review copy of [lsp-llc/local-ai-models#84](https://github.com/lsp-llc/local-ai-models/pull/84) (draft) so Jeff can open it from the work laptop without private GitHub or localhost.

| | |
|---|---|
| Source PR | [#84](https://github.com/lsp-llc/local-ai-models/pull/84) (draft, do not merge) |
| Head | `850bc76` on `cursor/community-monday-refresh-1d2a` |
| Base | `main` `69f7865` (132 catalog cards) |
| `generated_at` | `2026-09-07T13:34:09.040Z` |
| Snapshot `count` | **132** (= catalog) |
| Source | HuggingFace Hub public API only (`downloads`, `likes`, `id`) |
| ads.txt | **not touched** |
| Firebase | **not deployed** |
| X | **not scraped** |

Open: [sentiment.html](./sentiment.html)

`sentiment.html` fetches relative `./community-signals.json`. Last-updated **September 7, 2026** is baked in the static HTML (visible with JS off). Charts render from the same snapshot JSON.

This folder is `noindex`. Do not treat it as a merge vehicle for #84 or any other LAM PR.
